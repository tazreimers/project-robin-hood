# Feature Folders

Use this directory for feature-specific React components, hooks, and helpers once a page grows beyond simple composition.

Shared UI belongs in `frontend/components/`; API calls belong in `frontend/lib/api.ts`; shared types belong in `frontend/types/`.
